
import React from "react";
import ReactDOM from "react-dom";
import './index.css';
import App from './App';

// Use ReactDOM to render the 'App' component into the HTML element with the ID of 'root'.
ReactDOM.render(<App />, document.getElementById('root'));
